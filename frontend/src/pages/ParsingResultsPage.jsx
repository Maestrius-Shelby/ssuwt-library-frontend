import React from 'react';
import ParsingResults from '../components/UI/ParsingResults/ParsingResults';

const ParsingResultsPage = () => {
    return (
        <div>
            <ParsingResults />
        </div>
    );
};

export default ParsingResultsPage;